# Azure Container Registry removed - using GitHub Container Registry (ghcr.io) instead
# ghcr.io is free for public repositories and provides unlimited storage
# Authentication handled via GitHub Actions GITHUB_TOKEN
# Image format: ghcr.io/<owner>/<repo>/app-api:latest
